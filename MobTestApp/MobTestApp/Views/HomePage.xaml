<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MobTestApp.Views.HomePage"
             xmlns:vm="clr-namespace:MobTestApp.ViewModels"
             Title="{Binding Title}"
             Shell.TabBarIsVisible="False">

    <ContentPage.BindingContext>
        <vm:HomeViewModel />
    </ContentPage.BindingContext>

    <ContentPage.Resources>
        <ResourceDictionary>
            <Color x:Key="Accent">#96d1ff</Color>
        </ResourceDictionary>
    </ContentPage.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <StackLayout Grid.Row="0" VerticalOptions="FillAndExpand" HorizontalOptions="Fill">
            <StackLayout Orientation="Horizontal" HorizontalOptions="Center" VerticalOptions="Center">
                <ContentView Padding="0,20,0,20" VerticalOptions="FillAndExpand">
                    <Image Source="{AppThemeBinding Light=lightlogo.png, Dark=darklogo.png}" VerticalOptions="Center" HeightRequest="250" />
                </ContentView>
            </StackLayout>
        </StackLayout>

        <StackLayout Grid.Row="1" Orientation="Vertical" VerticalOptions="Center" Padding="10,0,10,0" Margin="100,0,100,0" Spacing="10">
            <Button     Margin="0" 
                            Text="What is Mōb?"
                            FontFamily="ProstoOne"
                            Clicked="About_Clicked"/>
            <Button     Margin="0" 
                            Text="Mōb Library"
                            FontFamily="ProstoOne"
                            Clicked="MobLibrary_Clicked"/>
            <Button     Margin="0" 
                            Text="Daily Dose"
                            FontFamily="ProstoOne"
                            Clicked="DailyDose_Clicked"/>
                <Button     Margin="0" Text="Settings (become a cog)"
                            Clicked="Settings_Clicked"
                            FontFamily="ProstoOne" />
            </StackLayout>
        

    </Grid>
</ContentPage>
